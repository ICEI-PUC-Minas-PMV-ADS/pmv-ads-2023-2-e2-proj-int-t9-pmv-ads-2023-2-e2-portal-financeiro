@model PORTAL.Models.Despesa

@{
    ViewData["Title"] = "Details";
}

<h1>Detalhes</h1>

<div>
    <h4>Despesa</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Conta)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Conta)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Mês)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Mês)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-info" asp-action="Edit" asp-route-id="@Model?.Id">Editar</a> |
    <a class="btn btn-success" asp-action="Index">Voltar</a>
</div>
